import React from 'react';
import ReactDOM from 'react-dom/client';
import app from './app'; // make sure filename matches

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
